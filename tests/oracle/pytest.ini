[pytest]
testpaths = .
markers =
    slow: marks tests as slow (image conversion, large inputs)
    category_a: deterministic text-to-text tools
    category_b: non-deterministic tools
    category_c: binary/image tools
addopts = -v --tb=short
